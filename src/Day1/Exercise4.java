/*
Red only: Exercise 4 (race condition)

In your own words, explain the difference between the two java keywords “synchronized” and “volatile”.

Synhronized means that only the stack that called the method/codepart can access it.

A volatile variable is a variable that is stored directly in the computers memory and not in the CPU cache

*/